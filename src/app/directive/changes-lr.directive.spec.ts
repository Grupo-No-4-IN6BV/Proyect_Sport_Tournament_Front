import { ChangesLRDirective } from './changes-lr.directive';

describe('ChangesLRDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangesLRDirective();
    expect(directive).toBeTruthy();
  });
});
